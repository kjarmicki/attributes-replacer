{
  "name": "attr-replacer",
  "version": "1.0.0",
  "description": "Chrome extension allowing for easy replacement of HTML attribute values (like anchor hrefs etc.)",
  "main": "index.js",
  "scripts": {
    "test": "karma start",

    "manifest": "node index.js",
    "contentscript": "browserify src/content-script.js -o dist/content-script.js",
    "popup": "browserify src/popup.js -o dist/popup.js && cp assets/popup.html dist/.",
    "background": "browserify src/background.js -o dist/background.js",
    "icon": "cp assets/temp-icon.png dist/.",

    "make": "npm run manifest && npm run contentscript && npm run popup && npm run background && npm run icon",
    "start": "npm run make"
  },
  "keywords": [
    "link",
    "url",
    "replacer"
  ],
  "author": "Krystian Jarmicki",
  "license": "MIT",
  "devDependencies": {
    "mocha": "2.3.3",
    "browserify": "11.2.0",
    "karma": "0.13.11",
    "karma-browserify": "4.4.0",
    "karma-chrome-launcher": "0.2.1",
    "karma-mocha": "0.2.0"
  },
  "dependencies": {
  }
}
